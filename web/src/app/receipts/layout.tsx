import type { Metadata } from "next";
export const metadata: Metadata = { title: "Receipts", description: "Every commitment a protected agent made to a person. Public by design." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
