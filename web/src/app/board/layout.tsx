import type { Metadata } from "next";
export const metadata: Metadata = { title: "Kept-rate board", description: "The share of promises each company's agents kept, as ruled by GenLayer validators." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
